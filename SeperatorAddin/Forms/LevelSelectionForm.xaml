<Window x:Class="SeperatorAddin.Forms.LevelSelectionForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Select Levels for Column Split"
        Height="500" Width="400" 
        MinHeight="400" MinWidth="350"
        Background="#F5F5F5"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C0C0C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0" Background="#DDD" Padding="5" MouseDown="Border_MouseDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Select Levels for Column Split" 
                           FontWeight="Bold" FontSize="14" 
                           VerticalAlignment="Center" HorizontalAlignment="Left" />
                <Button Grid.Column="1" Content="X" Width="25" Height="25" Padding="0"
                        FontWeight="Bold" FontSize="12" Background="Transparent" BorderThickness="0"
                        HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Click="btnClose_Click"/>
            </Grid>
        </Border>

        <!-- Instructions -->
        <Border Grid.Row="1" Background="#FFFFFF" Margin="10,10,10,5" CornerRadius="5" Padding="10">
            <TextBlock TextWrapping="Wrap" FontSize="12">
                <Run Text="Select the levels where you want to split the columns." FontWeight="Bold"/>
                <LineBreak/>
                <Run Text="The columns will be split at each selected level, creating segments between consecutive levels."/>
            </TextBlock>
        </Border>

        <!-- Level List -->
        <Border Grid.Row="2" Background="#FFFFFF" CornerRadius="5" Margin="10,5,10,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                    <CheckBox x:Name="cbSelectAll" Content="Select All" 
                              Checked="cbSelectAll_Checked" 
                              Unchecked="cbSelectAll_Unchecked"
                              FontWeight="Bold"/>
                </StackPanel>

                <Border Grid.Row="1" 
                        BorderThickness="1" 
                        BorderBrush="#CCCCCC" 
                        Background="White"
                        CornerRadius="5">
                    <ListView x:Name="lvLevels" 
                              Background="Transparent"
                              BorderThickness="0"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              SelectionMode="Extended">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                              Margin="0,0,10,0"
                                              Checked="LevelCheckBox_Changed"
                                              Unchecked="LevelCheckBox_Changed"/>
                                    <TextBlock Text="{Binding DisplayName}" 
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </Grid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="10,0,10,10">
            <Button x:Name="btnOK" Content="OK" Width="70" Margin="0,0,10,0"
                    Style="{StaticResource ModernButtonStyle}"
                    Click="btnOK_Click"/>
            <Button x:Name="btnCancel" Content="Cancel" Width="70"
                    Style="{StaticResource ModernButtonStyle}"
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>