<Window x:Class="SeperatorAddin.Forms.DuctSelectionForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SeperatorAddin"
        mc:Ignorable="d"
        Title="Select Ducts and Levels" Height="650" Width="550" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Ducts Section -->
        <TextBlock Grid.Row="0" Text="Select Ducts to Split:" Margin="10,10,10,5" FontWeight="Bold"/>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="10,0,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Filters -->
                <Grid Grid.Row="0" Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Filter:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Name="txtFilter" Margin="0,0,0,5" TextChanged="txtFilter_TextChanged"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="System:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" Name="cmbSystem" SelectionChanged="cmbSystem_SelectionChanged"/>
                </Grid>

                <!-- Ducts List -->
                <ListBox Grid.Row="1" Name="lbDucts" Margin="5" SelectionMode="Multiple" 
                         ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}" 
                                      Content="{Binding DisplayName}" 
                                      Checked="DuctCheckBox_Checked" 
                                      Unchecked="DuctCheckBox_Unchecked"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Selection Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                    <TextBlock Name="txtDuctCount" Text="0 selected" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Name="btnSelectAll" Content="Select All" Width="70" Margin="0,0,5,0" Click="btnSelectAll_Click"/>
                    <Button Name="btnSelectNone" Content="Select None" Width="70" Click="btnSelectNone_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Levels Section -->
        <TextBlock Grid.Row="2" Text="Select Levels (minimum 2):" Margin="10,10,10,5" FontWeight="Bold"/>

        <Border Grid.Row="3" BorderBrush="Gray" BorderThickness="1" Margin="10,0,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Levels List -->
                <ListBox Grid.Row="0" Name="lbLevels" Margin="5" SelectionMode="Multiple">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}" 
                                      Content="{Binding DisplayName}" 
                                      Checked="LevelCheckBox_Checked" 
                                      Unchecked="LevelCheckBox_Unchecked"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Level Count -->
                <TextBlock Grid.Row="1" Name="txtLevelCount" Text="0 selected" 
                           HorizontalAlignment="Left" Margin="5"/>
            </Grid>
        </Border>

        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Name="btnOK" Content="OK" Width="75" Margin="0,0,10,0" Click="btnOK_Click" IsDefault="True"/>
            <Button Name="btnCancel" Content="Cancel" Width="75" IsCancel="True" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>