<Window x:Class="SeperatorAddin.Forms.frmSeparator"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SeperatorAddin.Forms"
        mc:Ignorable="d"
        Title="Separator" Height="350" Width="400"
        WindowStyle="None"
        Background="#F5F5F5"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4CAF50"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f44336"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#d32f2f"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#DDD" Padding="5" MouseDown="Border_MouseDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="STools - Separator" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left" />
                <Button Grid.Column="1" Content="X" Width="25" Height="25" Padding="0"
                        FontWeight="Bold" FontSize="12" Background="Transparent" BorderThickness="0"
                        HorizontalAlignment="Right" VerticalAlignment="Center" Click="btnClose_Click"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock Text="Select elements to separate:" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Floors" VerticalAlignment="Center" FontSize="14"/>
                <TextBlock x:Name="FloorCount" Text="0 selected" FontStyle="Italic" Grid.Column="1" VerticalAlignment="Center" Margin="10,0" HorizontalAlignment="Right"/>
                <Button x:Name="SelectFloors" Content="Select" Grid.Column="2" Style="{StaticResource ActionButtonStyle}" Click="SelectFloors_Click" Width="70" Height="25" Padding="10,2"/>
            </Grid>
            <Grid Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Roofs" VerticalAlignment="Center" FontSize="14"/>
                <TextBlock x:Name="RoofCount" Text="0 selected" FontStyle="Italic" Grid.Column="1" VerticalAlignment="Center" Margin="10,0" HorizontalAlignment="Right"/>
                <Button x:Name="SelectRoofs" Content="Select" Grid.Column="2" Style="{StaticResource ActionButtonStyle}" Click="SelectRoofs_Click" Width="70" Height="25" Padding="10,2"/>
            </Grid>
            <Grid Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Walls" VerticalAlignment="Center" FontSize="14"/>
                <TextBlock x:Name="WallCount" Text="0 selected" FontStyle="Italic" Grid.Column="1" VerticalAlignment="Center" Margin="10,0" HorizontalAlignment="Right"/>
                <Button x:Name="SelectWalls" Content="Select" Grid.Column="2" Style="{StaticResource ActionButtonStyle}" Click="SelectWalls_Click" Width="70" Height="25" Padding="10,2"/>
            </Grid>
            <Grid Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Ceilings" VerticalAlignment="Center" FontSize="14"/>
                <TextBlock x:Name="CeilingCount" Text="0 selected" FontStyle="Italic" Grid.Column="1" VerticalAlignment="Center" Margin="10,0" HorizontalAlignment="Right"/>
                <Button x:Name="SelectCeilings" Content="Select" Grid.Column="2" Style="{StaticResource ActionButtonStyle}" Click="SelectCeilings_Click" Width="70" Height="25" Padding="10,2"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,15">
            <Button x:Name="Separate" Content="Separate" Style="{StaticResource ActionButtonStyle}" Click="Separate_Click" Width="100" Margin="0,0,10,0"/>
            <Button x:Name="Cancel" Content="Cancel" Style="{StaticResource CancelButtonStyle}" Click="btnCancel_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>