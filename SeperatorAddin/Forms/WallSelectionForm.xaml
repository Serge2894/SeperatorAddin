<Window x:Class="SeperatorAddin.Forms.WallSelectionForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SeperatorAddin"
        mc:Ignorable="d"
        Title="Select Levels for Wall Split" 
        Height="500" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="300"
        MinWidth="350">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,5"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title and Instructions -->
        <TextBlock Grid.Row="0" 
                   Text="Select Levels for Wall Split" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" 
                   Text="Select the levels where walls should be split. Walls will be created between each consecutive pair of selected levels." 
                   TextWrapping="Wrap" 
                   Margin="0,0,0,10"
                   Foreground="DarkGray"/>

        <!-- Level Selection Area -->
        <GroupBox Grid.Row="2" Header="Available Levels" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Select/Deselect All Buttons -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                    <Button Name="btnSelectAll" 
                            Content="Select All" 
                            Click="SelectAll_Click" 
                            Width="80" 
                            Height="25" 
                            Margin="0,0,5,0"/>
                    <Button Name="btnDeselectAll" 
                            Content="Deselect All" 
                            Click="DeselectAll_Click" 
                            Width="80" 
                            Height="25"/>
                </StackPanel>

                <!-- Scrollable Level List -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="5">
                    <StackPanel Name="levelStackPanel">
                        <!-- CheckBoxes will be added programmatically -->
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </GroupBox>

        <!-- Selected Count -->
        <TextBlock Grid.Row="3" 
                   Name="txtSelectedCount" 
                   Text="0 levels selected" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,10"
                   FontStyle="Italic"/>

        <!-- OK/Cancel Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                    Name="btnOK" 
                    Content="OK" 
                    Click="OK_Click" 
                    IsDefault="True"
                    FontWeight="Bold"/>

            <Button Grid.Column="1" 
                    Name="btnCancel" 
                    Content="Cancel" 
                    Click="Cancel_Click" 
                    IsCancel="True"/>
        </Grid>
    </Grid>
</Window>